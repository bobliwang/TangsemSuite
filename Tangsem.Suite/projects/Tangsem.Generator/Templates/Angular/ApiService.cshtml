@* Generator: Template *@
@inherits MultipleTableMetadataTemplate

@using Tangsem.Common.Extensions
@using Tangsem.Generator.Templates
@using Tangsem.Generator.Settings;


export class @(Configuration.RepositoryName)ApiService {

    construct(private httpClient: HttpClient) {
    }


@foreach (var tableMetadata in this.TableMetadatas)
{<text>

     
     public get@(tableMetadata.EntityName)List(): Observable<@(tableMetadata.EntityName)[]> {
        return this.httpClient.get<@(tableMetadata.EntityName)[]>(`_api/repo/@(tableMetadata.EntityName)`);
     }
     
     public get@(tableMetadata.EntityName)ById(id: number | string): Observable<@(tableMetadata.EntityName)> {
        return this.httpClient.get<@(tableMetadata.EntityName)>(`_api/repo/@(tableMetadata.EntityName)/${id}`);
     }
     
     public update@(tableMetadata.EntityName)(id: number | string, model: @(tableMetadata.EntityName)): Observable<@(tableMetadata.EntityName)> {
        return this.httpClient.post<any>(`_api/repo/@(tableMetadata.EntityName)/${id}`, model);
     }
     
     public create@(tableMetadata.EntityName)(id: number | string): Observable<@(tableMetadata.EntityName)> {
        return this.httpClient.post<@(tableMetadata.EntityName)>(`_api/repo/@(tableMetadata.EntityName)`);
     }


 </text>}

}
